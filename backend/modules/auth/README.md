# auth 模块

`auth` 模块负责本地单用户登录和接口保护。

## 职责

- 提供登录相关 API。
- 复用现有认证依赖。
- 保护需要登录的后端接口。

## 边界

本模块不负责聊天、人设编辑、人物包读写和数据库迁移。

如果其他模块需要登录保护，应复用已有认证依赖，
不要重新实现一套登录逻辑。

## 注意事项

- 不要改变当前登录主流程，除非任务明确要求。
- 不要在业务模块里复制登录校验代码。
- 不要把本地用户信息写入人物包。

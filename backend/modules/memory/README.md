# Memory Module

Compatibility wrapper for existing long-term memory APIs.

This module is separate from the future `relationship_memory` layer. Existing
memory behavior is unchanged in this migration.


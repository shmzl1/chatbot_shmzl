# voice 模块

`voice` 模块负责可选语音能力。

## 职责

- 读取当前角色的语音配置。
- 调用 GPT-SoVITS 服务。
- 返回生成的语音文件信息。

## 人物依赖

语音配置来自角色包中的 `voice` 字段。

如果 `ref_audio_path` 是相对路径，应相对于当前角色包目录解析。

## 边界

语音能力是可选能力。

语音服务不可用时，不应破坏普通文字聊天。

## 注意事项

- 不要硬编码旧角色包路径。
- 不要提交语音素材和模型权重。
- 参考音频缺失时应返回明确错误。

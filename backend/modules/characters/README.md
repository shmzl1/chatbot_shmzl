# Characters Module

Compatibility wrapper for character pack APIs.

`character_api.py` still contains character routes and the existing
`/characters/{character_id}/persona-review/...` routes. They are intentionally
left in place during this low-risk migration.

